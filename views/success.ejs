<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Registration Successful</title>
    <style>
        .alert {
            padding: 10px;
            margin-bottom: 15px;
            border-radius: 5px;
            text-align: center;
        }

        .alert-success {
            color: #3c763d;
            background-color: #dff0d8;
            border-color: #d6e9c6;
        }

        .hidden {
            display: none;
        }

        .pass11 {
            height: 100px;
            width: 100px;
            margin-top: 50px;
            display: flex;
            justify-content: center;
            background-color: red;
        }
    </style>
    <script>
        function redirectToHome() {
            setTimeout(() => {
                window.location.href = "/HackUtsav";
            }, 5000); 
        }

        function clearForm() {
            document.getElementById('registrationForm').reset();
        }

        window.onload = function() {
            clearForm();
            redirectToHome();
        };
    </script>
</head>
<body>
    <div class="alert alert-success">
        Registration successful!
    </div>

    <div class="pass">
        <h1>Here Is your entry Pass :</h1>
    </div>

    <div class="pass11">pass</div>
</body>
</html>
